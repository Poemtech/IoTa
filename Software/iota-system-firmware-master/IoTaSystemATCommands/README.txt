Note:   When working one a new version, set the version in the project settings for the
        compiler, execute a "clean" and then build. The "variant" does not build on a
        rebuild, but that is OK as long as the above step is done whenever the
        build version changes.